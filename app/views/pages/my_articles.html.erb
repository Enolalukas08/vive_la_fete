<div class="container container-booking">
  <figure class="text-center">
    <blockquote class="blockquote">
      <h1>Liste de vos articles ! 🤪</h1>
    </blockquote>
    <p small>
      Hey <cite title="Source Title"><%= current_user.first_name %></cite>, tes articles t'attendent pour faire la fête !
    </p>
  </figure>
  <div class="image-container">
    <img class="rounded-image" style="text-align: center;" src="https://usagif.com/wp-content/uploads/upgifsok/party-10.gif" alt="Image d'arrière-plan"/>
  </div>
  <div class="row">
    <% @my_articles.each do |article| %>
      <div class="col-md-6 d-flex flex-column align-items-center">
        <div class="card position-relative card-booking-articles mb-3 card-radius" style="max-width: 540px;">
          <div class="card-bg-image-my-articles mx-auto mt-2" style="background-image: <%= article.photo.present? ? 'url(' + cl_image_path(article.photo.key) + ')' : 'linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(' + image_path('logo.png') + ')' %>"></div>
          <div class="card-body">
            <h5 class="card-title text-center"><%= article.name.capitalize %></h5>
            <p class="card-text"> <strong>Description :</strong><%= article.description %></p>
            <h6 class="card-text text-center"><strong>Prix :</strong> <%= article.price %> €</h6>
            <div class="text-center">
                  <%= link_to 'Modifier', edit_article_path(article), class: 'btn btn-primary btn-show col-md-6' %>
                  <%= link_to "Supprimer", article_path(article), class: 'btn btn-danger btn-show col-md-6',  data: {turbo_method: :delete, turbo_confirm: "Êtes-vous sûr de vouloir supprimer cet article ?" } %>
              </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
